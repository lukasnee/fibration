# TODO List

- Write a `pre-commit` hook that prints out ROM/RAM usage increase/decrease
  after each commit. Try to maintain the solution `extern/ln` library as much as
  possible.
- Rename all folders names from PascalCase to camelCase.
